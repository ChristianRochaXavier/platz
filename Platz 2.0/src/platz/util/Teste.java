package platz.util;

public class Teste {

	public static void main(String args[]) {
		
		/*	NOT DELETE
		EntityManager entityManager = JPAUtil.getEntityManager();
		
		List<Conta> contas = entityManager.createQuery("select new platz.model.Conta(login, email) from Conta").getResultList();
		
		for (int i = 0; i < contas.size(); i++) {	
			System.out.println(contas.get(i).getLogin() + "<--");
			System.out.println(contas.get(i).getEmail() + "<--");
			System.out.println(contas.get(i).getPerfil() + "<--");
		}

		entityManager.close();
*/
		
		
		/*
		 * CategoriaEvento categoria = new CategoriaDAO().buscarPorId(1);
		 * Empresa empresa = new EmpresaDAO().buscarPorId(1);
		 * 
		 * List<Evento> eventosCategoria = new
		 * EventoDAO().listarPorCategoria(categoria); List<Evento>
		 * eventoDestaque = new EventoDAO().listarDestaques(); List<Evento>
		 * eventoEmpresa = new EventoDAO().listarPorEmpresa(empresa);
		 * 
		 * for(Evento evento: eventoEmpresa){
		 * 
		 * System.out.println(evento.getEvento());
		 * System.out.println(evento.getId()); System.out.println();
		 * 
		 * 
		 * }
		 */

	}

}
